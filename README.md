# DisplayGraphicsCodeGenerator
Cross platform application that generates the header with the code to includes (first implementation for C) the data to display a graphic in a specified display driver.
